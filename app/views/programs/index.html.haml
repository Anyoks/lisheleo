%section.content-header
  %h1
    Lishe Leo Programs
    %small All Programs in the database
  %ol.breadcrumb
    %li
      %a{:href => "#"}
        %i.fa.fa-dashboard
        Level
    %li.active Here


%br


%br
#partial_page
  =render "programs/table", programs: @programs #the table that was previously here was extracted to the partial to be DRY. seems like I needed it more than I thought! :-)

%br
-if current_admin && current_admin.is_previlaged?
  %p= link_to 'New Program', new_program_path, class: "btn btn-primary" #, :remote => true, 'data-toggle' =>  "modal", class: "btn btn-primary"


